# Archived V8 Atomic MERGE Tools

This folder preserves the v8 atomic MERGE / learned-overlay tools for
traceability. They are no longer active project tools.

The current production path is `scripts/pipeline/run_v8_layout_reconstruction.py`
and does not load GNN checkpoints or learned MERGE overlays.

If this branch is reopened later, move the needed tools back deliberately and
create a new run tag. Do not silently import these scripts from production code.
