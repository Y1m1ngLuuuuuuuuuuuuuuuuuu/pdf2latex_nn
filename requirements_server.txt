# PDF2LaTeX-NN server / AutoDL extras
#
# Install after requirements.txt when the machine is used for data collection,
# optional MinerU/PaddleOCR-adjacent processing, or API/VLM baselines.

-r requirements.txt

# arXiv / object storage helpers
boto3>=1.37,<2.0
awscli>=1.32,<2.0

# Optional OCR / detector / formula-recognition stack.
# The current project path consumes MinerU outputs; these packages are not
# required if MinerU is installed separately or outputs are copied from AutoDL.
ultralytics>=8.4,<9.0
paddleocr>=3.0,<4.0
paddlepaddle-gpu>=3.0,<4.0; platform_system == "Linux"
pix2tex>=0.1,<0.2

# Optional API baseline SDK.  Real API calls are still guarded by
# ALLOW_API_CALLS=1 in the pipeline.
openai>=1.70,<2.0
